# Python Adapters

This directory contains the remaining Python adapter scripts used by Scriberr.

## Voxtral

Voxtral uses its local adapter scripts in this folder.

## Diarization

PyAnnote + Sortformer are now served by the gRPC diarization engine in:

```bash
asr-engines/scriberr-diariz-torch
```
