export function ScriberrLogo({ className = "" }: { className?: string }) {
  return (
    <div className={`${className}`}>
      <span className="text-2xl font-semibold text-gray-900 dark:text-gray-50 tracking-wide uppercase">
        SCRIBERR
      </span>
    </div>
  )
}